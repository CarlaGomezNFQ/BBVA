<aura:component description="NotifyOppTeamFormDecision" implements="lightning:availableForFlowScreens" access="global" controller="DES_IP_NotifyInsideFormDecisions">
    <aura:attribute name="targets" type="GroupMember[]" />
    <aura:attribute name="decision" type="String" />
    <aura:attribute name="form" type="Inside_information_form__c" />
    <aura:attribute name="relatedCase" type="Case" />

    <aura:handler name="init" value="{!this}" action="{!c.doInit}" />

    <div class="spinnerHolder">


        <lightning:spinner alternativeText="Loading" size="large" variant="brand"/>

    </div>
</aura:component>