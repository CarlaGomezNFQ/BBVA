/* eslint-disable no-unused-expressions */
({
  handleMessage : function(cmp, event, helper) {
    helper.handleMessage(cmp, event);
  },
  doNextComponent: function(cmp, event, helper) {
    helper.doNextComponent(cmp, event);
  },
  doCancelFlow: function(cmp, event, helper) {
    helper.doCancelFlow(cmp, event);
  }
});