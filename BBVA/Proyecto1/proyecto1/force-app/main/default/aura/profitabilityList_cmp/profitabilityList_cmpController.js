({
  doInit: function(cmp, evt, helper) {
    helper.doInit(cmp, evt, helper);
  },
  handleCreate: function(cmp, evt, helper) {
    helper.handleCreate(cmp, evt, helper);
  },
  viewRecord: function(cmp, evt, helper) {
    helper.viewRecord(cmp, evt, helper);
  },
  btnCancel: function(cmp, evt, helper) {
    helper.btnCancel(cmp, evt, helper);
  }
});