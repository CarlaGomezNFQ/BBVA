({
  doInit: function(component, event, helper) {
    helper.getCurrencyUnits(component, event, helper);
  },
  showModal: function(component, event, helper) {
    helper.showModalCmp(component, event, helper);
  }
});